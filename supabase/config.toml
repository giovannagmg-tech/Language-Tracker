project_id = "lxxtusxgneqxmuavdcrg"

[db.seed]
enabled = true
# Só conteúdo que não depende de conta nenhuma. O CLI grava o hash de cada
# arquivo de seed e nunca mais o executa — nada condicional pode morar aqui.
sql_paths = ["./seed.sql"]
